{
  "name": "@hasna/draw",
  "version": "0.1.0",
  "description": "Headless board SDK for quick notes and freehand drawings, like Google Keep. A framework agnostic board and card model with a portable Excalidraw compatible drawing scene, JSON import and export, plus optional React board, card, and draw surface components",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "draw": "dist/cli/index.js"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./react": {
      "types": "./dist/react/index.d.ts",
      "import": "./dist/react/index.js"
    }
  },
  "files": [
    "dist",
    "docs/*.md",
    "LICENSE",
    "README.md"
  ],
  "scripts": {
    "clean": "rm -rf dist",
    "build": "bun run clean && bun run build:lib && bun run build:react && bun run build:cli && bun run build:types",
    "build:lib": "bun build src/index.ts --outdir dist --target node --external nanoid",
    "build:react": "NODE_ENV=production bun build src/react/index.ts --outdir dist/react --target browser --external react --external react-dom --external 'react/jsx-runtime' --external 'react/jsx-dev-runtime' --external nanoid",
    "build:cli": "bun build src/cli/index.ts --outdir dist/cli --target node --external commander --external chalk --external nanoid",
    "build:types": "tsc -p tsconfig.build.json --emitDeclarationOnly --outDir dist",
    "build:dashboard": "cd dashboard && bun run build",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "dev:cli": "bun run src/cli/index.ts",
    "prepack": "bun run build",
    "prepublishOnly": "bun run typecheck && bun test",
    "postinstall": "mkdir -p $HOME/.hasna/draw 2>/dev/null || true"
  },
  "keywords": [
    "draw",
    "drawing",
    "notes",
    "note",
    "keep",
    "whiteboard",
    "sketch",
    "freehand",
    "excalidraw",
    "board",
    "canvas",
    "react",
    "sdk",
    "hasna"
  ],
  "author": "hasna",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hasna/draw.git"
  },
  "homepage": "https://github.com/hasna/draw",
  "bugs": {
    "url": "https://github.com/hasna/draw/issues"
  },
  "engines": {
    "bun": ">=1.0.0"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org",
    "access": "public"
  },
  "dependencies": {
    "chalk": "^5.4.1",
    "commander": "^13.1.0",
    "nanoid": "^5.1.5"
  },
  "peerDependencies": {
    "react": ">=18",
    "react-dom": ">=18"
  },
  "peerDependenciesMeta": {
    "react": {
      "optional": true
    },
    "react-dom": {
      "optional": true
    }
  },
  "devDependencies": {
    "@types/bun": "^1.2.4",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "typescript": "^5.7.3"
  }
}
